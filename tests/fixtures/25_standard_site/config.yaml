site:
  title: Standard Site Demo
  url: https://example.com

# No `atproto:` block: the verification artifacts (`<link
# rel="site.standard.document">` per post, `.well-known/site.standard.publication`)
# are on by default, deriving their AT-URIs from the ITALIC_ATPROTO_DID env var
# (set by the test harness) + `site.url`.

# Keep the fixture focused on the derived proofs: no sitemap, no feeds.
sitemap:
feed: []
